#!/bin/sh
# Generated by Meson: pins the interpreter that provides PyGObject and points it
# at the application's private module directory.
PYTHONPATH="@pythondir@${PYTHONPATH:+:$PYTHONPATH}"
export PYTHONPATH
exec "@python@" -m markdown_vault.main "$@"
